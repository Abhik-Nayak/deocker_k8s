apiVersion: v1
kind: Service
metadata:
  name: auth-server
spec:
  selector:
    app: auth-server                # must match the Pod label, not the Deployment name
  ports:
    - port: 4000
      targetPort: 4000
