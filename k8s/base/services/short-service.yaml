apiVersion: v1
kind: Service
metadata:
  name: short-server
spec:
  selector:
    app: short-server                # must match the Pod label, not the Deployment name
  ports:
    - port: 5000
      targetPort: 5000
